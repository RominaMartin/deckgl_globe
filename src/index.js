import React from "react";
import ReactDOM from "react-dom";
import JourneyContainer from "./Journey/Containers/JourneyContainer";

ReactDOM.render(
  <React.StrictMode>
    <JourneyContainer />
  </React.StrictMode>,
  document.getElementById("root")
);
